apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tourpackage-be-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: 2306240111-be.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tourpackage-be-service
                port:
                  number: 80

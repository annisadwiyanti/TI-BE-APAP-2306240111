apiVersion: v1
kind: Service
metadata:
  name: tourpackage-be-service
  namespace: default
spec:
  selector:
    app: tourpackage-be
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
